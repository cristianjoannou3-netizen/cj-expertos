'use client'

// Push notifications disabled — Service Worker (Serwist) temporarily removed
// because @serwist/next does not support Turbopack (Next.js 16 default bundler).
// Re-enable when @serwist/turbopack is stable or use configurator mode.
export default function PushRegistration() {
  return null
}
